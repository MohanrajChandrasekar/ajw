import { NumNotDotDirective } from './num-not-dot.directive';

describe('NumNotDotDirective', () => {
  it('should create an instance', () => {
    const directive = new NumNotDotDirective();
    expect(directive).toBeTruthy();
  });
});
